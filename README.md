# moodle-local_change_auth
Change Auth is a local Moodle plugin that automatically converts user accounts using the email authentication method to manual authentication through a scheduled daily task.
